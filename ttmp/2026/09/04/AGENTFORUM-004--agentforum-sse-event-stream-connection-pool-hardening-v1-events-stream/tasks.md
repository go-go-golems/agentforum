# Tasks

## TODO

- [x] S1: pool hardening — SetMaxOpenConns(8)+idle, N-poller concurrency test with before/after timings <!-- t:t8er -->
- [x] S2: server endpoint — GET /v1/events/stream (Flusher, heartbeat, shared denormalization), streaming test suite <!-- t:r2yb -->
- [x] S3: client switch — fetch ReadableStream reader in useEventStream, reconnect/backoff, browser verification <!-- t:1ka8 -->
- [x] S4: docs — help entries (unified-inbox, web-ui), README, full gate, reMarkable bundle <!-- t:xtul -->
